spring:
  thymeleaf:
    mode: HTML5
logging:
  level:
    org:
      springframework: error
server:
  port: ${PORT:5000}

qiniu:
  accessKey: ${ACCESSKEY}
  secretKey: ${SECRETKEY}
  domain: ${DOMAIN}